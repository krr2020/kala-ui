# Kala UI — Full Audit & Remediation Plan (2026-08-27)

Status snapshot at audit time: type-check ✅ · biome lint ✅ · 3381 tests ✅ · build ✅ · storybook build ✅.
All dependencies were upgraded to latest as part of this audit (see "Completed in this pass").

**Update 2026-08-27 (later): Phase 1 executed — all 11 P0 runtime bugs (C1-C11) fixed with regression tests.** See the Phase 1 section.

**Update 2026-08-27 (latest): Phase 2 executed — theming & customization overhaul (T1-T11) + class-name consistency pass.** See the Phase 2 section. Verification after: 3406 react tests (164 files) · 22 hooks tests · biome check clean · build ✅ · storybook ✅.

---

## Part 1 — What was completed in this pass (dependency modernization)

| Area | From → To |
|---|---|
| All 28 Radix primitives | various → latest minors (1.2.x / 2.3.x etc.) |
| react / react-dom | 19.2.3 → 19.2.8 (peers relaxed `~19.2.3` → `^19.2.0`) |
| lucide-react | 0.561 → **1.34** (brand icons removed — stories migrated to `simple-icons`) |
| framer-motion | 12.42 → **13.1** |
| react-day-picker | 9.13 → **10.0** (calendar `table` class key → `month_grid`) |
| apexcharts / react-apexcharts | 4.7/1.9 → **7.0 / 2.1** |
| react-resizable-panels | 3.0 → **4.12** (full API rename — wrapper rewritten: `Group`/`Panel`/`Separator`, `orientation`, `panelRef`/`groupRef`, string-percentage sizes) |
| storybook | 8.5 → **10.5** (`addon-essentials`/`interactions`/`links` removed; `@storybook/test` → `storybook/test`; argTypesRegex actions removed; main.ts/preview.tsx migrated) |
| vite / @vitejs/plugin-react | 6.3 / 5.1 → **8.2 / 6.1** |
| vitest (both pkgs) | 4.1.5 / 2.1.9 → **4.1.11** (aligned) |
| typescript | 5.9.3 → **7.0.2** (native compiler; `baseUrl` removed from tsconfigs) |
| tailwindcss / @tailwindcss/postcss | 4.1 → **4.3** |
| biome | 2.3.11 → **2.5.10** |
| jsdom, testing-library, @types/*, sonner, date-fns, input-otp, simple-icons, tsdown, autoprefixer, postcss | latest |

Bugs fixed along the way:
- **Duplicate React 19.2.3/19.2.8 instances** (tilde-pinned peers + stale lockfile entry) — broke 307 tests. Fixed by relaxing peer ranges to `^19.2.0` and reinstalling.
- **`use-local-storage` rewritten**: infinite re-render loop with inline object/array `defaultValue` (identity-unstable `useCallback` deps); `setValue` stale-closure losing sequential updates; storage sync overwriting defaults on missing keys. Options now kept in refs; functional-updater-safe.
- **`use-toggle`**: missing `option2` dependency.
- Storybook preview `hsl(var(--background))` body styles (invalid under whole-value token format) → `var(--background)`.
- Biome 2.5 fallout: stale suppression comments removed; genuine unused vars/imports fixed; test files get a scoped override for `noExplicitAny`/`noNonNullAssertion`/unused vars.
- Sonner 2.0.8 keeps its toast store across tests → `afterEach(() => toast.dismiss())`.
- jsdom 30 normalizes `4rem` → `64px` in computed styles → 2 skeleton test assertions updated.

`pnpm outdated -r` now reports **0 outdated packages**.

---

## Part 2 — Findings register (by severity)

### P0 — Critical bugs (break at runtime for real usage)

| # | Component | Location | Bug |
|---|---|---|---|
| C1 | Charts (all) | `charts/chart.tsx:7` | Static `import ReactApexChart from "react-apexcharts"` — apexcharts touches `window` at module scope; **any SSR framework crashes** on first render of any chart, and the root barrel transitively imports it. Needs dynamic `import()` / `next/dynamic` wrapper. |
| C2 | TagInput | `tag-input.tsx:148-164` | Pasting multiple tags keeps only the last — `addTag` loops over stale `value` prop within one tick. |
| C3 | TagInput | `tag-input.tsx:198` | `key={…${Date.now()}…}` — every tag remounts every render (DOM churn, kills transitions). |
| C4 | NumberInput | `number-input.tsx:25-44,68-78` | Controlled mode: typing `-` or `1.` commits parsed number and erases intermediate input — **can't type negatives/decimals**; `isControlled` flips when parent stores `undefined`. |
| C5 | useHotkeys | `react-hooks/use-hotkeys/use-hotkeys.ts:5-24` | `mod+K` / `meta+K` matchers can never fire (documented example is non-functional). |
| C6 | useFocusTrap | `react-hooks/use-focus-trap/use-focus-trap.ts` | Focus escapes via disabled/hidden focusables; no initial focus, no restore. |
| C7 | SortableHandle | `dnd/dnd.tsx:410` | `useSortable({ id: "" })` — phantom sortable; handle-based dragging broken + duplicate-id collisions. |
| C8 | useTableState | `data-table/useTableState.ts:242-252` | Debounced server search effect has **no dep array** — any re-render during the debounce window cancels the pending `onSearchChange` (silent data loss in server mode). |
| C9 | DataTable | `data-table/data-table.tsx:160-162` | `selection.selectedIds` prop only seeds initial state — controlled selection never syncs (bulk-delete flows see stale UI). |
| C10 | NavLink | `nav-link/nav-link.tsx:99,109` | Controlled `opened` prop ignored for chevron/children rendering (uses internal state) — controlled nav can never expand. |
| C11 | Header | `header/header.tsx:89` | `useScrollLock(isMobileMenuOpen)` passes a live value to an initialState-only API — scroll lock never engages (or never releases). |

### P1 — High (broken customization / a11y blockers / API traps)

**Theming & customization (the core ask):**
| # | Location | Issue |
|---|---|---|
| T1 | `styles/globals.css:249-278`, `helpers.css:22-39` | `.theme-input/.theme-dropdown/.theme-popover/.theme-card` use `!important` on bg/border/shadow — **user `className` overrides cannot win** on Card, Input, Select, Popover, DropdownMenu, RadioGroup. |
| T2 | `charts/theme-utils.ts:30-95` | Charts hardcode 4 hex palettes; custom `--primary` etc. never reach charts; wrapper-level theme classes invisible (reads only `documentElement`); `#fff` labels on light custom themes; `getCurrentTheme()` unguarded `document` (SSR risk at `utils.ts:16`). |
| T3 | `data-table/*.tsx` (6 sites: `data-table.tsx:563,572`, `data-table-header-row.tsx:67,103`, `data-table-skeleton.tsx:179,201`) | `border-border-strong` utility **does not exist** — silent no-op borders. |
| T4 | Root `THEMING.md` | Actively wrong: HSL-triplet examples (`--background: 210 40% 98%`) are invalid under the whole-value format; `hsl(var(--x))` snippets broken; `colors.gray[100]`/`spacing.md`/`typography.text.lg` don't exist; `tailwind-base` default-import doesn't exist; v3-only host instructions for a v4 library. `TOKEN_SPEC.md` is the accurate doc. |
| T5 | `design-tokens/themes.ts` | Dead code that contradicts `globals.css` (accent bg blue-vs-white, high-contrast borders inverted, shadow alpha 0.05-vs-0). Also `designTokens` stub export typed `{} as`. |
| T6 | Scrims | `dialog.tsx:42`, `alert-dialog.tsx:42`, `drawer.tsx:55`, `sidebar.tsx:95`, `header.tsx:270` hardcode `bg-black/50|20`; `banner.tsx:103` `hover:bg-black/10` — no `--overlay` token. |
| T7 | `rating.tsx:139` | `text-yellow-400 fill-yellow-400` hardcoded star color. |
| T8 | No `ThemeProvider`/`useTheme`; no `prefers-color-scheme` support | Hosts hand-roll `.dark` toggling; docs show a fake "provider". |
| T9 | `--radius` documented but consumed nowhere; `--kala-radius-*` (the real knobs) undocumented. | |
| T10 | v4 host story undefined | Consuming apps passing extra Tailwind utilities via `className` get nothing unless they compile CSS themselves — needs explicit docs + `@custom-variant dark` guidance. |
| T11 | `tailwind.config.ts` | `content` key ignored under v4 `@config` (saved by `@source`); safelist lists non-utilities (no-op); v3-compat shim inside v4 build. |

**Accessibility blockers:**
| # | Location | Issue |
|---|---|---|
| A1 | `radio-group.tsx:131,160,201` | `<Label htmlFor={props.id}>` with no `useId` fallback — unnamed, unclickable-label radios in default usage. `useId` is used in exactly one file across 106 components. |
| A2 | `field/field.tsx` | `FieldDescription`/`FieldError` never wired via `aria-describedby`/`aria-errormessage` — Field can't produce an accessible control without manual ids. |
| A3 | `combobox.tsx:95-108` | `role="button"` span nested inside trigger `<button>`; Space not prevented (scrolls page); clear control also toggles popover (no stopPropagation). `multi-select.tsx:213,244,264` has the same nested-button problem (real `<button>` in `<button>`). |
| A4 | `tree-view.tsx:78,86` | Double tab stop per node (`li` + inner `div`), keyboard handler unreachable on the first; no `role="group"` on child `<ul>`; no Up/Home/End navigation. |
| A5 | `input.tsx:108` | Password-visibility toggle `tabIndex={-1}` — keyboard users can never reveal password. |
| A6 | `data-table-body-rows.tsx:72-79` | `role="button"` on `<tr>` destroys table semantics; no aria-label. |
| A7 | `segmented-control.tsx:88-133` | No radiogroup semantics, no arrow keys, no focus-visible styles. |
| A8 | `steps.tsx:110-137` | Completed steps have empty accessible name (bare Check icon, no aria-label); no list semantics. |
| A9 | `list.tsx:124-141` | `tabIndex={0}` + no keyboard handler / no role on interactive `li`. |
| A10 | `sidebar.tsx:94-98` | Mobile overlay: no Escape, no focus trap, background scrolls; section headings rendered as `<Button disabled>`. |
| A11 | `header.tsx:264+` | Fullscreen mobile menu: no Escape, no focus trap, `:focus` (not `:focus-visible`) rings. |
| A12 | Systemic | 31 interactive files lack `focus-visible` styles; `.focus-ring` sets `outline: none` unconditionally (globals.css:320). |
| A13 | `file-upload.tsx:113-136,166` | `<input type=file>` inside `<button>`; clear button icon-only without aria-label; error `<p>` not `role="alert"`/describedby. |
| A14 | `copy-button.tsx:37-43` | No `.catch` on clipboard write (unhandled rejection); timers never cleared (setState-after-unmount, stacked resets). |
| A15 | `password-strength-indicator.tsx:58-96` | No `aria-live` on strength changes. |
| A16 | `avatar.tsx:71-96` | `alt` not required/defaulted; `cva()` called inside render (perf); `sizeMap` maps `md`→2.5rem but real `md`=3rem (`config/avatar.ts:8`) — skeleton wrong size for `md`/`2xl`. |

**API consistency traps:**
| # | Location | Issue |
|---|---|---|
| V1 | sizes | 3 mid-scale tokens (`default`/`md`/`base`) and 3 xs-stories coexist; `config/avatar.ts:4-12` has BOTH `default` and `md` with different values. |
| V2 | color axis | `variant` means color on Button/Badge/Alert; `color` on Tag/Text/Progress; `danger`≡`destructive`≡`error` aliases ship in public unions (button.ts:5-13, badge.ts:5-14, tag.tsx:18-26, progress remap at progress.tsx:41). |
| V3 | callbacks | `onValueChange` vs `onChange` vs `onDateChange` all in use (tabs/rating/pagination/tag-input/color-input/segmented-control use `onChange`). |
| V4 | controlled/uncontrolled | Combobox, MultiSelect, DatePicker are controlled-only; SegmentedControl syncs via effect anti-pattern; TimePicker `period` never resyncs with controlled `value` (AM/PM stuck); TagInput renders nothing uncontrolled despite optional `value`. |
| V5 | refs | ~12 components drop `ref`/rest entirely: Rating, TreeView, EmptyState, SessionCard, AvatarGroup, SocialLoginButtons, PasswordStrengthIndicator, Timeline, PageTransition, UserMenuDropdown, Sidebar, Header/Footer roots. Mixed forwardRef + ref-as-prop styles. |
| V6 | Button `asChild`+disabled | `disabled` forwarded to `<a>` (invalid attr) when asChild. |
| V7 | Flex/Box `[key: string]: any` | Silent prop swallowing — in-repo instance: `header.tsx:216` `gap={1.5}` emits nothing. |
| V8 | `cn` not exported from root barrel | Consumers must know `@kala-ui/react/lib/utils`. `buttonVariants` exported but `inputStyles` etc. not (inconsistent). |
| V9 | date-picker `{...(props as any)}` | `date-picker.tsx:71,155` — spread after core props can shadow `selected`/`onSelect`/`disabled`; the 2 real `any`s in shipped src. |

### P2 — Medium

- **DataTable**: bulk actions resolve `selectedRows` against current-page `data` only (server mode acts on a subset of what the UI claims, `data-table.tsx:157,253-256`); controlled `pagination.page` ignored; `pathname.startsWith(href)` false-positives in navigation.tsx:47-52 + sidebar.tsx:83-88.
- **MultiSelect**: search not reset on close (multi-select.tsx:211).
- **TimePicker**: `scrollIntoView({behavior:"smooth"})` scrolls whole page (time-picker.tsx:26-28); `period` stale (see V4).
- **FileUpload**: re-selecting same file after clear doesn't fire change (`e.target.value` never reset, file-upload.tsx:62-68); `accept` not validated on drop path.
- **TagInput**: regex-injection via `separators` (raw strings into `RegExp`); no IME composition guard; no `defaultValue`.
- **NumberInput**: `{...props}` overrides internal `onKeyDown/onBlur/onChange` (spread order, number-input.tsx:147-155); float drift on step (`0.30000000000000004`).
- **Alert**: dismissal permanent for component lifetime — reused Alert for a new message stays hidden (alert.tsx:47,73-78).
- **Banner**: `role="banner"` (page landmark) + no `aria-live` default (banner.tsx:66).
- **Spoiler**: measures overflow once at mount — async content never shows "Show more" (needs ResizeObserver, spoiler.tsx:40-44).
- **Collapse**: no className/style/id surface at all (aria-controls impossible).
- **ErrorBoundary**: renders `undefined` (blank) when no `fallback`; no `resetKeys`.
- **TreeView**: context object recreated per render + no memo — full-tree re-render per toggle.
- **List**: interactive items render `<button>/<a>` as direct `<ul>` children (invalid HTML, dividers break); `line-clamp-${lines}` dynamic class never compiled (list.tsx:361).
- **Navigation**: `mobileLayout="vertical"` hides desktop nav entirely (navigation.tsx:70-101).
- **AppShell**: `navbar.breakpoint` config never read; mobile collapse lacks breakpoint prefix; `Main` always reserves navbar width (app-shell.tsx:139-153,228-244).
- **Rating**: `readOnly` removes from a11y tree + half-values unreachable by keyboard (rating.tsx:117).
- **RadioGroup buttons variant**: hidden radio, no focus-within styling (radio-group.tsx:209-217).
- **ColorInput**: preset swatches unnamed; native color picker inside dismissable popover.
- **Input**: suffix container missing `pointer-events-none` (input.tsx:100).
- **DatePicker**: popover never closes after selection; `input-otp.tsx:20` `any`.
- **Hooks**: `use-click-outside` exports unused options type; `use-scroll-lock` no refcount (two consumers unlock each other) + scrollTop captured after lock; `use-hover` callback-ref pattern broken with `useRef` consumers; `useLocalStorage` cross-tab `storage` echo of own writes (dispatches with no newValue — currently filtered); only 3/36 hooks tested.
- **Packaging**: `lib/animations.ts` (~330 lines) + `lib/use-mobile.ts` dead but shipped in dist; `design-system/` showcase compiled into published tarball (no exports entry — dead weight pulling all types); **`src/primitives/command/` is NOT an orphan (corrected during Phase 2): it's a second, internal command-palette implementation consumed by Combobox/MultiSelect/DnD — duplicating the public `components/command`; merge candidates for Phase 5**; sonner theme prop never passed.
- **Docs**: root THEMING.md (T4); `--ring-offset-color` vs `ring-offset DEFAULT` two sources of truth; `popover.ts:45-50` legacy variants `border-white/20`; `progress.tsx:26` rgba white stripes.
- **Chart empty state** `height: ${x}px` double-units when height is a string (chart.tsx:58).

---

## Part 3 — Phase-wise remediation plan

### Phase 0 — Baseline hardening (done in this pass ✅)
Dependency modernization to latest (incl. all majors), duplicate-React fix, `use-local-storage`/`use-toggle` fixes, SB10 migration, TS7. **Everything below starts from a green, fully-current baseline.**

### Phase 1 — Critical runtime bugs (P0) · ~3-5 days — **DONE (2026-08-27)**
1. ✅ C1 charts SSR: `React.lazy(() => import("react-apexcharts"))` + Suspense (ChartSkeleton fallback); `getCurrentTheme()` guarded for no-DOM. Charts barrel now imports cleanly with `window`/`document` undefined (regression-tested).
2. ✅ C2/C3 TagInput: paste batches into ONE `onChange` with in-batch dedup + maxTags; stable keys (`${tag}-${index}`); user `onKeyDown`/`onPaste` composed (run first, `defaultPrevented` opts out); separator strings regex-escaped.
3. ✅ C4 NumberInput: `type="text" inputMode="decimal"` + focused string buffer (shows "-", "1." while typing), live-commit on complete numbers, clamp+commit on blur/Enter, garbage reverts on blur; user `onKeyDown`/`onBlur` composed; internal handlers can no longer be clobbered by the rest-spread.
4. ✅ C5 useHotkeys: `mod` normalized to meta (Apple) / ctrl (elsewhere) BEFORE parsing; all modifiers must match exactly (ctrl≠meta). 8 new tests incl. platform stubs.
5. ✅ C6 useFocusTrap: filters disabled/hidden/`aria-hidden`/`display:none` focusables; guards empty traps; moves focus in on activation; restores pre-trap focus on release/unmount. 4 new tests.
6. ✅ C7 SortableHandle: receives parent SortableItem's attributes/listeners/`setActivatorNodeRef` via context; no phantom `useSortable({id:""})` registration; standalone render warns in dev.
7. ✅ C8 useTableState debounce: effect deps `[searchQuery, debounceMs]` + latest-callback ref — unrelated re-renders no longer cancel the pending `onSearchChange`. Regression test drives a re-render mid-window.
8. ✅ C9 DataTable: `selection.selectedIds` prop changes now sync into state during render ("adjust state on prop change") — bulk-delete flows see fresh UI. NavLink controlled `opened` now drives chevron + children.
9. ✅ C10 NavLink controlled `opened` (see 8).
10. ✅ C11 Header: scroll lock driven through the setter in an effect; `useScrollLock` itself no-ops unlock when never locked (was scrolling pages to top on mount — any Header mount).

### Phase 2 — Theming & customization (the "easy custom styling" ask) · ~1-2 weeks — **DONE (2026-08-27)**
1. ✅ T1 `!important` removal + class rename: all custom classes renamed to collision-proof `kala-*` names (`kala-surface-card` / `kala-surface-input` / `kala-surface-popover`, `kala-focus-ring[-destructive|-success]`, `kala-focus-within-ring[-destructive]`, `kala-ring[-destructive]`), defined once in `helpers.css` inside `@layer components` (utilities always outrank them — `className` overrides win, no `!important` anywhere). `globals.css` now imports `helpers.css` (single source; copy-drift bugs gone). 54-file codemod.
2. ✅ Class-name anomalies fixed: dead misnamed duplicate `focus-visible-ring-success` and unused `ring-success` deleted; `theme-dropdown` (painted `--card`) merged into `kala-surface-popover` (`--popover`) — all 8 sites were popover surfaces; `helpers.css:116` copy-drift bug (`--ring-offset-color` in the success ring) eliminated with the dedup.
3. ✅ T2 charts read live CSS tokens: `theme-utils.ts` resolves `var(--primary)` etc. through a probe element with `color-mix` tint/shade ramps (cache keyed on `<html>` class list); curated hex fallbacks when no CSS engine (SSR/jsdom). 5 new resolution tests. `#fff` labels → `var(--destructive-foreground)` / `var(--primary-foreground)`.
4. ✅ T3 `--border-strong` token + `@theme` mapping — the 6 data-table sites that referenced a nonexistent utility now compile.
5. ✅ T6 `--overlay` + `--overlay-alpha` tokens; all scrims tokenized (dialog, alert-dialog, drawer, sidebar `bg-overlay`; header `bg-overlay/40`; banner `hover:bg-overlay/20`).
6. ✅ T7 rating stars → `text-warning fill-warning`.
7. ✅ T5 `design-tokens/themes.ts` (contradicted globals.css) and the broken `designTokens` stub export deleted — CSS is the single source of truth for theme values. **Follow-up (same day): the entire `@kala-ui/design-tokens` package was removed** — after Phase 2 nothing imported it (the `@theme` block in globals.css carries the palette ramps, fonts, text scale, shadows and container widths). Root `build:tokens` script and all doc references cleaned up.
8. ✅ T8 `ThemeProvider` + `useTheme` shipped: 6 themes + `system`, class strategy on `<html>`, localStorage persistence, live `prefers-color-scheme`, `color-scheme` sync; SSR-safe; 10-test suite.
9. ✅ T11 Tailwind v4-native: `globals.css` rebuilt on `@theme` / `@custom-variant dark` / `@plugin "tailwindcss-animate"`; the v3 `@config` shim, `tailwind.config.ts`, `src/config/tailwind-base.ts`, the `./config/tailwind-base` export, `autoprefixer`, and the `@kala-ui/design-tokens` dependency are all deleted. v4 scale drift pinned (shadows keep v3 values, `--text-*--line-height` pairs, `--container-*` + `max-w-screen-*`→`max-w-*` codemod, base-layer `--tw-ring-color`/`--tw-ring-offset-color` defaults). Also fixed: `animate-fade-in` was referenced 4× but never compiled (now a real keyframe).
10. ✅ T4/T9/T10 THEMING.md rewritten from scratch (whole-value token format, full token reference incl. `--kala-*` shape knobs and `--overlay`, ThemeProvider usage, both v4 host recipes: precompiled CSS vs `@source`+helpers self-compile); TOKEN_SPEC/README stale `tailwind-base` instructions removed; dead `--radius` token deleted.
    Acceptance met: a consumer overriding the CSS vars sees correct colors in every component including charts, scrims and rating; `className` overrides win everywhere (cascade layers, verified in built CSS).

### Phase 3 — Accessibility · ~1-2 weeks
1. A1/A2: `useId` wiring for RadioGroup labels and Field description/error (`aria-describedby`, `aria-errormessage`).
2. A3: restructure Combobox/MultiSelect triggers (no nested interactives; clear stops propagation).
3. A4 TreeView: single roving tabindex, `role=group`, arrow/Home/End nav.
4. A5-A9: password toggle tabIndex, DataTable row semantics, SegmentedControl radiogroup, Steps naming, List keyboard support.
5. A10-A12: sidebar/header overlays (Escape/focus/scroll-lock), global `focus-visible` pass over the 31 files; make `.focus-ring` visible-focused only.
6. A13-A16: file-upload structure + labels; copy-button `.catch` + timer cleanup; live regions (password strength, banner); avatar alt + skeleton sizeMap + hoist cva.
7. Re-enable disabled biome a11y rules (`noStaticElementInteractions`, `noArrayIndexKey`) and fix fallout instead of suppressing.

### Phase 4 — API consistency (pre-1.0, breaking allowed) · ~1-2 weeks
1. V1/V2: single size scale (`xs|sm|md|lg|xl`), single color axis (`variant` = fill style, `color` = semantic color), deprecate-not-remove alias values in v0.x.
2. V3: standardize `onValueChange` (+ keep `onChange` as deprecated alias where HTML-compatible).
3. V4: every stateful component gets value/defaultValue/onChange duality via `useUncontrolled`; delete the reimplementations (tabs' `useControllableState`, copy-button's inline clipboard → use hooks package).
4. V5: ref+rest pass on the 12 components; settle on ref-as-prop (React 19).
5. V6/V7/V9: Button asChild disabled handling; replace Flex/Box `any` index signatures with real prop types; type date-picker spreads properly.
6. V8: export `cn`, all `*Styles`/`*Variants` objects from root barrel.
7. Hooks package: fix `use-click-outside` options API, `use-scroll-lock` refcount, `use-hover` ref; version react-hooks vitest/jsdom alignment (done); tests for 33 untested hooks.

### Phase 5 — Component quality (P2 backlog) · ~2 weeks, parallelizable
DataTable server-mode selection/pagination semantics; MultiSelect/FileUpload/TagInput/TimePicker fixes (list above); Spoiler ResizeObserver; ErrorBoundary fallback + resetKeys; TreeView perf; List semantics + static line-clamp map; Navigation/AppShell layout logic; Rating readOnly a11y; hooks cleanup; packaging hygiene (exclude `design-system/`, `lib/animations.ts`, `lib/use-mobile.ts` from dist; decide sonner `theme` prop; `dist/primitives` orphan).

### Phase 6 — Release readiness · ~3-5 days
1. Changesets or similar versioning; CI (lint+type+test+build+storybook on PR).
2. Visual regression harness (Storybook test-runner / Playwright) over themes × core components.
3. Consumer integration test: scaffold a Next.js App Router app importing via package exports, SSR every component, custom theme vars — the real "works across all projects" gate.
4. Publish `0.1.0-beta` with migration notes.

### Suggested sequencing rationale
Phase 1 first (data-loss/crash bugs), Phase 2 next because it's the stated adoption blocker (styling across all projects), Phase 3 before wide rollout (a11y debt compounds once apps copy patterns), Phase 4 while still pre-1.0 (breaking changes get free), Phases 5-6 in parallel tracks.
